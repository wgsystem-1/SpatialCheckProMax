<UserControl x:Class="SpatialCheckProMax.GUI.Views.CompactValidationProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1400">
    
    <UserControl.Resources>
        <Style x:Key="CompactCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.1" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="StageCard" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.08" Direction="270"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="MetricLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="FontWeight" Value="Medium"/>
        </Style>
        
        <Style x:Key="MetricValue" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>
    </UserControl.Resources>
    
    <Grid Background="#F3F4F6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- 상단 바: 전체 진행률 + 핵심 지표 -->
        <Border Grid.Row="0" Style="{StaticResource CompactCard}" Margin="20,20,20,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- 헤더 -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Orientation="Vertical">
                        <TextBlock Text="검수 진행 중" FontSize="20" FontWeight="Bold" Foreground="#1F2937"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <Border x:Name="CurrentFileBadge"
                                    Background="#DBEAFE"
                                    Padding="8,2"
                                    CornerRadius="6"
                                    Margin="0,0,8,0"
                                    Visibility="Collapsed">
                                <TextBlock x:Name="CurrentFileBadgeText"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           Foreground="#1D4ED8"/>
                            </Border>
                            <TextBlock x:Name="CurrentFileText" 
                                      Text="" 
                                      FontSize="14"
                                      FontWeight="SemiBold"
                                      Foreground="#2563EB"
                                      Visibility="Collapsed"/>
                        </StackPanel>
                        <TextBlock x:Name="CurrentFilePathText"
                                  Text=""
                                  FontSize="12"
                                  Foreground="#6B7280"
                                  Margin="0,2,0,0"
                                  Visibility="Collapsed"/>
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock x:Name="EstimatedEndTimeText" 
                                      Text="" 
                                      FontSize="12"
                                      Foreground="#6B7280"
                                      Margin="0,0,15,0"/>
                            <TextBlock x:Name="RemainingWorkText"
                                      Text=""
                                      FontSize="12"
                                      Foreground="#6B7280"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <Button Grid.Column="1" x:Name="StopButton" 
                           Content="검수 중지" 
                           Padding="16,8"
                           Click="StopValidation_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#EF4444"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="FontWeight" Value="Medium"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
                
                <!-- 진행률 바 -->
                <Grid Grid.Row="2" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <ProgressBar Grid.Column="0" x:Name="ProgressBar" 
                                Height="8" 
                                Background="#E5E7EB"
                                Foreground="#3B82F6"
                                BorderThickness="0"
                                Margin="0,0,16,0"/>
                    
                    <TextBlock Grid.Column="1" x:Name="ProgressPercentageText" 
                              Text="0%" 
                              FontSize="18" 
                              FontWeight="Bold"
                              Foreground="#3B82F6"
                              VerticalAlignment="Center"/>
                </Grid>
                
                <!-- 배치 타임라인 -->
                <Border x:Name="BatchTimelineContainer"
                        Grid.Row="1"
                        Background="#F8FAFC"
                        CornerRadius="8"
                        Padding="12"
                        Margin="0,0,0,12"
                        BorderBrush="#E0F2FE"
                        BorderThickness="1"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="파일 진행 상황" Style="{StaticResource CaptionText}" FontWeight="SemiBold"/>
                        <ItemsControl x:Name="BatchTimeline" Margin="0,8,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="8" Margin="0,2,0,2" Background="{Binding BackgroundBrush}">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Index, StringFormat='[{0}]'}" 
                                                       FontWeight="SemiBold"
                                                       Foreground="#1F2937"/>
                                            <TextBlock Text=" " />
                                            <TextBlock Text="{Binding FileName}"
                                                       FontWeight="SemiBold"
                                                       Foreground="#1F2937"
                                                       ToolTip="{Binding FilePath}"/>
                                            <TextBlock Text=" " />
                                            <TextBlock Text="{Binding StatusText}"
                                                       Foreground="{Binding StatusBrush}"
                                                       Margin="8,0,0,0"
                                                       FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Summary}"
                                                       Foreground="#6B7280"
                                                       Margin="12,0,0,0"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- 핵심 지표 -->
                <UniformGrid Grid.Row="3" Columns="5" Rows="1">
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="현재 단계" Style="{StaticResource MetricLabel}"/>
                        <TextBlock x:Name="CurrentStageText" Text="-" Style="{StaticResource MetricValue}" Foreground="#1F2937"/>
                    </StackPanel>
                    
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="소요 시간" Style="{StaticResource MetricLabel}"/>
                        <TextBlock x:Name="ElapsedTimeText" Text="00:00:00" Style="{StaticResource MetricValue}" Foreground="#1F2937" FontFamily="Consolas"/>
                    </StackPanel>
                    
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="예상 남은 시간" Style="{StaticResource MetricLabel}"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="EstimatedTimeText" Text="계산 중..." Style="{StaticResource MetricValue}" Foreground="#1F2937" FontFamily="Consolas"/>
                            <TextBlock x:Name="SpeedIndicatorText" Text="" FontSize="11" Foreground="#6B7280" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="완료 단계" Style="{StaticResource MetricLabel}"/>
                        <TextBlock x:Name="CompletedStagesText" Text="0 / 6" Style="{StaticResource MetricValue}" Foreground="#10B981"/>
                    </StackPanel>
                    
                    <StackPanel>
                        <TextBlock Text="발견된 오류" Style="{StaticResource MetricLabel}"/>
                        <TextBlock x:Name="TotalErrorsText" Text="0" Style="{StaticResource MetricValue}" Foreground="#EF4444"/>
                    </StackPanel>
                </UniformGrid>
            </Grid>
        </Border>
        
        <!-- 중단 영역: 단계별 카드 (균등 배치) -->
        <UniformGrid Grid.Row="1" Columns="6" Rows="1" Margin="20,0,20,16" x:Name="StageCardsPanel">
            <!-- 단계별 카드가 동적으로 추가됨 - UniformGrid로 균등 배치 -->
        </UniformGrid>
        
        <!-- 하단 영역: 현재 단계 상세 정보 -->
        <Border Grid.Row="2" Style="{StaticResource CompactCard}" Margin="20,0,20,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- 상세 정보 헤더 -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" x:Name="DetailHeaderText" 
                              Text="현재 단계 상세 정보" 
                              FontSize="16" 
                              FontWeight="SemiBold" 
                              Foreground="#1F2937"/>
                    
                    <Button Grid.Column="1" x:Name="ToggleDetailButton" 
                           Content="▲ 접기" 
                           Click="ToggleDetail_Click"
                           Background="Transparent"
                           BorderThickness="0"
                           Foreground="#6B7280"
                           FontSize="12"
                           Cursor="Hand"/>
                </Grid>
                
                <!-- 상세 정보 내용 -->
                <ScrollViewer Grid.Row="1" x:Name="DetailScrollViewer" 
                             VerticalScrollBarVisibility="Auto"
                             MaxHeight="300">
                    <StackPanel x:Name="DetailContentPanel">
                        <!-- 현재 처리 중인 항목 정보 -->
                        <Border Background="#F9FAFB" CornerRadius="6" Padding="12" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Column="0" Text="📍" FontSize="16" Margin="0,0,8,0"/>
                                <StackPanel Grid.Column="1">
                                    <TextBlock x:Name="CurrentProcessingText" 
                                              Text="대기 중..." 
                                              FontSize="13" 
                                              Foreground="#374151"
                                              FontWeight="Medium"/>
                                    <TextBlock x:Name="CurrentProgressText" 
                                              Text="" 
                                              FontSize="11" 
                                              Foreground="#6B7280"
                                              Margin="0,2,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        
                        <!-- 실시간 통계 -->
                        <Border Background="#F0F9FF" CornerRadius="6" Padding="12" Margin="0,0,0,8">
                            <UniformGrid Columns="4" Rows="1">
                                <StackPanel Margin="0,0,10,0">
                                    <TextBlock Text="처리된 항목" FontSize="10" Foreground="#6B7280"/>
                                    <TextBlock x:Name="ProcessedItemsText" Text="0" FontSize="16" FontWeight="Bold" Foreground="#3B82F6" Margin="0,2,0,0"/>
                                </StackPanel>
                                <StackPanel Margin="0,0,10,0">
                                    <TextBlock Text="전체 항목" FontSize="10" Foreground="#6B7280"/>
                                    <TextBlock x:Name="TotalItemsText" Text="0" FontSize="16" FontWeight="Bold" Foreground="#1F2937" Margin="0,2,0,0"/>
                                </StackPanel>
                                <StackPanel Margin="0,0,10,0">
                                    <TextBlock Text="처리 속도" FontSize="10" Foreground="#6B7280"/>
                                    <TextBlock x:Name="ProcessingSpeedText" Text="0/초" FontSize="16" FontWeight="Bold" Foreground="#10B981" Margin="0,2,0,0"/>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="이 단계 남은 시간" FontSize="10" Foreground="#6B7280"/>
                                    <TextBlock x:Name="StageRemainingTimeText" Text="-" FontSize="16" FontWeight="Bold" Foreground="#F59E0B" Margin="0,2,0,0"/>
                                </StackPanel>
                            </UniformGrid>
                        </Border>
                        
                        <!-- 완료된 단계별 검수 결과 요약 -->
                        <Border x:Name="PartialResultsPanel" Background="#ECFDF5" CornerRadius="6" Padding="12" Margin="0,0,0,8" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Text="✅ 완료된 단계 검수 결과" FontSize="12" FontWeight="SemiBold" Foreground="#065F46" Margin="0,0,0,8"/>
                                
                                <ItemsControl x:Name="StageResultsList">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,4">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="120"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="0" Text="{Binding StageName}" FontSize="11" Foreground="#374151" FontWeight="Medium"/>
                                                <TextBlock Grid.Column="1" FontSize="11" Foreground="#DC2626">
                                                    <Run Text="오류 "/>
                                                    <Run Text="{Binding ErrorCount}" FontWeight="Bold"/>
                                                    <Run Text="개"/>
                                                </TextBlock>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                
                                <Border Background="#FEF2F2" CornerRadius="4" Padding="8" Margin="0,8,0,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🎯 누적 오류: " FontSize="12" Foreground="#991B1B" FontWeight="Medium"/>
                                        <TextBlock x:Name="CumulativeErrorCountText" Text="0" FontSize="12" Foreground="#DC2626" FontWeight="Bold"/>
                                        <TextBlock Text=" 개" FontSize="12" Foreground="#991B1B"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>

